using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Saltarecena : MonoBehaviour
{
    // Start is called before the first frame update
   public void Next()
    {
        Application.LoadLevel(1);
    }
}
